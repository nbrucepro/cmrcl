version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dd-client
    ports:
      - '3002:3002'
    env_file:
      - .env
    restart: unless-stopped

